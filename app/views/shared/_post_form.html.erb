<%= form_with(model: @post, local: true) do |f| %>
<%= render 'shared/error_messages', object: f.object %>

<%= f.text_area :content, placeholder: 'input new post', class: 'form-control' %>

<%= f.submit 'create new post', class: 'btn btn-primary' %>
<% end %>